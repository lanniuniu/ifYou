/**
 * 光标移动
 * 1、正常按方向键是一个字母
 * 2、option + 左右 让移动的粒度变成单词
 * 3、cmd + 左右 让移动的粒度变成行
 * 4、cmd+shift+\ 让移动的粒度变成代码块
 * 5、cmd+上下 让移动的粒度变成文档
 * 
 */
function a() {
    a++
    return a
}
function b() {
    a++
    return a
}

/**
 * 文本选择、删除
 * shift选中
 * 同理
 * 
 * 更多选择 可以配置 自定义按键
 */